{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ローカルサーバー起動",
      "type": "shell",
      "command": "open -a 'Google Chrome' http://localhost:3000 && yarn install && yarn run dev"
    }
  ]
}
